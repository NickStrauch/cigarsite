<%= form_for(@cigar) do |f| %>
  <% if @cigar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cigar.errors.count, "error") %> prohibited this cigar from being saved:</h2>

      <ul>
      <% @cigar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brand_name %><br>
    <%= f.text_field :brand_name %>
  </div>
  <div class="field">
    <%= f.label :mfgr %><br>
    <%= f.text_field :mfgr %>
  </div>
  <div class="field">
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
